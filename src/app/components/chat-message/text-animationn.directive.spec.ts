import { TextAnimationnDirective } from './text-animationn.directive';

describe('TextAnimationnDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAnimationnDirective();
    expect(directive).toBeTruthy();
  });
});
