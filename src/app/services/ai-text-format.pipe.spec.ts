import { AiTextFormatPipe } from './ai-text-format.pipe';

describe('AiTextFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AiTextFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
